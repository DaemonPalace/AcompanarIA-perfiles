# Real-data model calibration pipeline (requirements.md Stage 1+).
# server.py/engine.py/generator/* were stdlib-only through v1.2.0 of the schema;
# this file is new as of Stage 1 — see requirements.md §2 for why that constraint
# was deliberately dropped (Option B: trained models load directly into engine.py).
pandas==3.0.5
numpy==2.5.2
scipy==1.18.1
scikit-learn==1.9.0
pgmpy==1.1.2
